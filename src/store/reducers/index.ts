const initialState = {
	images: [],
	folders: [],
	tags: [],
	selectedImage: null,
	selectedFolder: null
};

const rootReducer = ( state = initialState, action: any ) => {
	return state;
};

export default rootReducer;
